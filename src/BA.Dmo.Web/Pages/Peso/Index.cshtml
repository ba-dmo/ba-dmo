@page "/peso"
@model BA.Dmo.Web.Pages.Peso.IndexModel
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Policy = BA.Dmo.Web.Authorization.ModulePolicies.Peso)]
@{
    ViewData["Title"] = "Peso";
}
<main data-testid="page-peso-operador">
    <h1>Peso — Operador</h1>
    <p>Experiência Operador (módulo peso sem peso.aprovar — UD-06/UD-15). O conteúdo do módulo Peso é entregue pela sua unidade própria (U-10).</p>
</main>
