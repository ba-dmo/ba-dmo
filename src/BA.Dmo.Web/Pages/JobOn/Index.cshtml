@page "/jobon"
@model BA.Dmo.Web.Pages.JobOn.IndexModel
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Policy = BA.Dmo.Web.Authorization.CapabilityPolicies.JobonView)]
@{
    ViewData["Title"] = "Job On";
}

<!-- U-13 — Job On landing & folha (Plan-V3 modules/05; UD-16).
Reference-first implementation per portal-dmo-design-final/job-on-v48-folha-producao.html.
No behavior or design invented—consumed via dmo:date-select/calendar components (U-09).

Note: Shell/header/nav are provided by _Layout.cshtml + _Navigation.cshtml. 
This page only contains the main content area.
-->

<main data-testid="page-jobon">
    <h1>Job On</h1>
    <p>Landing global em Modo consulta (UD-16). O conteúdo do módulo Job On é entregue pela sua unidade própria (U-13).</p>
</main>
